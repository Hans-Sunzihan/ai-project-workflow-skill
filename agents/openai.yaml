interface:
  display_name: "AI Project Workflow"
  short_description: "Bootstrap, plan, review, and hand off project work."
  default_prompt: "Use $ai-project-workflow to bootstrap this project session, plan a version, run an explicit review, or persist a handoff."

policy:
  allow_implicit_invocation: false
